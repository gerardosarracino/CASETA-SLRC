<?xml version="1.0" encoding="UTF-8"?>
<templates>
	<!-- <t t-extend="ActionpadWidget">
        <t t-jquery=".actionpad" t-operation="replace">
		<div class="actionpad">
                <button t-attf-class='button set-customer #{ ( widget.pos.get_matricula() and widget.pos.get_matricula().matricula.length > 10) ? "decentered" : "" }'>
                    <i class='fa fa-car' role="img" aria-label="Matricula" title="Matricula" />
                    <t t-if="widget.pos.get_matricula()">
                        <t t-esc="widget.pos.get_matricula().matricula" />
                    </t>
                    <t t-if="!widget.pos.get_matricula()">
                    Matricula
                </t>
                </button>
                <button class='button pay'>
                    <div class='pay-circle'>
                        <i class='fa fa-chevron-right' role="img" aria-label="Pay" title="Pay" />
                    </div>
                    Payment
                </button>
			</div>
        </t>
    </t> -->
</templates>
